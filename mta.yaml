_schema-version: "3.2"
ID: empproj
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: empproj-destination-content
  type: com.sap.application.content
  requires:
  - name: empproj-destination-service
    parameters:
      content-target: true
  - name: empproj-repo-host
    parameters:
      service-key:
        name: empproj-repo-host-key
  - name: empproj-uaa
    parameters:
      service-key:
        name: empproj-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: empproj_html_repo_host
          ServiceInstanceName: empproj-html5-srv
          ServiceKeyName: empproj-repo-host-key
          sap.cloud.service: empproj
        - Authentication: OAuth2UserTokenExchange
          Name: empproj_uaa
          ServiceInstanceName: empproj-xsuaa-srv
          ServiceKeyName: empproj-uaa-key
          sap.cloud.service: empproj
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: empproj-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: empproj-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - empproj.zip
      name: empproj
      target-path: resources/
- name: empproj
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: empproj-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: empproj-destination-service
    service-plan: lite
- name: empproj-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: empproj-xsuaa-srv
    service-plan: application
- name: empproj-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: empproj-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
